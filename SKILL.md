---
name: mac-maintenance
description: Use when a user requests a Mac or macOS maintenance check, software/driver/firmware update review, storage organization, cache cleanup, or removal of obsolete applications and background components.
---

# Mac 检查与维护

先取得这台机器的当前证据，再完成已有授权内的更新、整理和清理。以安装版本、实际运行状态、目标变化和磁盘读数交付结果；目录很大、软件很旧或名称陌生，都不是单独的处理依据。

## 1. 接续范围与授权

- 先读当前任务的报告、执行记录和用户决定，刷新将要操作的项目。旧清单用于定位，不能当作当前状态。
- 若存在 `config/preferences.local.json`，优先读取它；否则读取 [通用默认配置](config/preferences.json)。再合并当前任务的决定，当前明确指令优先。脚本不会自动选择 local 文件，调用时用 `--preferences` 显式传入选定配置。用户新增排除时立即撤下相关待执行动作；临时决定留在任务中，用户要求今后沿用才更新个人配置，不能把个人排除写成通用禁令。
- 用户只要求检查时保持只读；用户已经要求更新、整理、清理时，核实明确目标后继续执行，不逐项重复确认。能靠检查补齐的证据先自行补齐。
- 涉及范围明显扩大、用户数据去留未决定、未授权的停机/重启或其他难以恢复的变更时，先完成独立工作，再提出具体的待决事项。某项受阻不阻塞其他已授权操作。
- 从本次任务识别需保护的项目、未提交修改、媒体与工程库、数据库、备份、活动下载、外接卷及远程连接。保护项按当前授权更新，不把本次路径、版本或临时保留决定变成长期限制。

## 2. 取得基线

按请求覆盖硬件/电池/存储、macOS 版本与更新通道、应用与架构、外设固件、驱动/扩展、启动项、磁盘占用及备份状态。具体命令与易错点见 [检查与执行参考](references/inspection-and-actions.md)，需要相应操作时读取。

优先使用附带的 `scripts/audit.py --quick` 取得可重复基线；需要系统在线更新查询时加 `--online`，需要外设、扩展、备份和服务详情时去掉 `--quick`。脚本只覆盖报告中列出的检查，不能把它当作完整硬件诊断。运行时发现、输出路径和字段见 [脚本用法](references/script-usage.md)；遇到已完成的当前证据不重复扫描。

- 应用盘点读取 bundle ID、短版本、build、架构、安装来源和更新 feed；限深遍历应用目录，不按文件修改日期或名字推断版本。
- 目录检查先看元数据、用途、大小和活动状态，按盘与路径限深；记录权限错误和未覆盖区域，不把不可读记成零。
- 识别当前连接使用的远程软件、VPN/代理、网络扩展及监听服务。例如 UU 远程正在承载会话时，维护方案应保留该连接路径。
- 保存检查时间、命令退出状态及必要原始证据。公开报告省略序列号、账号、令牌等无关信息；默认不读取个人文件内容。

## 3. 确认可匹配的更新

为每个候选记录：**已装版本/build → 目标版本/build、官方来源及查询时间、平台/架构/系统要求、稳定或测试通道、安装方式**。

| 对象 | 决策依据 |
|---|---|
| macOS 与 Apple 内置驱动/固件 | 查询系统提供的匹配更新；保留用户当前通道，单独识别跨大版本、切换测试通道和重启影响 |
| App Store / 官方应用更新器 | 核对 Mac 产品与安装来源；App Store 的相同 bundle ID 可能返回 iOS 产品，不能直接比较 |
| Sparkle / 官方发布页 | 遍历候选再筛平台、通道和系统要求，按产品版本规则比较；feed 没有固定排序，旧官网文章也可能滞后 |
| Homebrew / 开发工具 | 核对依赖、reverse dependencies、运行服务和实际将被更新的列表；逐项执行明确范围，避免笼统升级连带影响项目 |
| 专业创作软件 / 数据库 | 先核实实际项目库、目录、活动状态与备份；涉及格式迁移或不可回退时，先完成可访问的备份/导出并查兼容说明 |
| 外设驱动 / 固件 | 核对厂商、精确型号、硬件修订、连接方式和适用系统；通过该产品官方工具或系统支持方式处理 |

没有匹配更新是有效的完成结果。源不可访问、平台不符、离线外设或无法读回版本时写“未确认”，不能用另一个平台的新版本填空。对专业软件使用厂商的合法授权更新途径，不替换许可组件或修改签名来强行安装。

配置中的 `skip_update_bundle_ids` 排除对应应用更新；`manager_only_bundle_prefixes` 要求使用指定管理器提供的更新。管理器目录里的“安装”、试用、订阅推荐不能当成已安装产品的更新。应用清理与更新排除分别判断，仍以当前用户范围为准。

## 4. 形成可执行清单并推进

每次变更先记录 **对象/精确路径、当前证据、操作与理由、已有授权、预期影响、恢复方式、前后验证**。这份清单用于执行与交付，不应成为对已授权操作新增的一轮审批。

对已定位的清理目标，使用 `cleanup.py preview` 保存精确清单，执行前用 `verify` 刷新元数据与偏好。`review` 表示待完成维护判断；`unchanged: true` 只表示本次复查一致，不代表授权、可重建性或句柄已验证。`blocked`/退出码 2 时自行补齐证据、缩小目标或重新生成预览，继续已有授权内的工作。脚本不执行删除；具体动作仍按下述维护流程与所属工具完成。大型目录达到扫描上限时缩小到实际目标，不把不完整快照绕成通过。

**更新：** 优先普通安全补丁。核实应用活动状态和未保存工作，通过原安装来源/官方更新器执行。下载完成、安装器退出成功、应用包已替换、运行进程已使用新版是不同状态。需要重新打开应用、重启服务或系统时，依据已有授权与连接影响安排；仍运行旧版就如实标记。

**整理与卸载：** 先确认重复版本是否被工程、插件、命令行路径或文件关联使用。功能重叠不等于无用；启动项未加载、主应用未出现在常见目录也不等于残留。关联驱动、helper、许可与网络组件时优先官方卸载器，并验证主程序和依赖关系。

删除旧版前，验证新版实际启动、许可/关键功能可用，并检查工程、插件、CLI、文件关联和共享组件等适用依赖；“存在更高版本”不足以证明替代品可用。`replacement` 中的可用性与依赖字段必须来自实际证据，不能为让脚本通过而填写。复杂版本格式、不同 bundle ID 的官方替代迁移按产品证据单独核实；脚本保守比较失败不等于产品不兼容。

**清理：** 对明确授权的精确目标，复核真实路径、所属卷、类型、大小/数量、可重建依据、相关进程和目标句柄。检查完整后才执行定向清理；目录型缓存通常保留容器，只清其已核实内容。路径变化、符号链接越界、挂载点、活动写入或权限错误出现时，暂停该项并重新核实。使用厂商/包管理器支持的定向清理；自写操作必须约束在已验证目标，不提供一键广域删除。

- 历史会话、旧下载分片、工程库、图库目录、Gallery、模板/插件、旧版本软件需要用途和用户数据去留证据，不能仅因体积或日期转成缓存。用户明确授权其整理/删除时，按该授权评估并处理。
- 模拟器 `Shutdown` 不证明其数据可删除。`PRBPosterExtensionDataStore` 等大型存储先查所属设备、用途和内部组成，继续深挖其中准确的可重建缓存边界；隔离时保留描述符、原资源和应用测试数据，验证关键功能与数据后，按恢复需要和授权释放。缓存尚未生成时记录“重建未验证”，不把启动成功当作重建成功。某次发现的文件名/glob 不成为后续自动删除许可，不能直接擦除设备来缩小目录。
- 受保护的 macOS 认证弹窗交给用户直接输入；不接收、代输或绕过密码。命令受权限、Xcode 许可或工具缺失限制时记录实际原因，不为完成维护暗改系统保护或自动接受许可。工具政策明确拒绝某一路径时，不更换工具绕过该拒绝；可以评估在授权与政策内独立可行的官方更新方式。继续不依赖该阻碍的事项。

## 5. 验证与交付

- 更新后读回版本/build；适用时验证签名、正常启动、实际运行版本，以及服务、网络连接和项目库访问。只读体检不能证明压力测试或硬件诊断通过。
- 清理后复查目标数量/大小、错误输出、保护对象与相关服务；同一卷采集操作前后 `df`。把“目标减少量”和“卷可用空间变化”分别报告。
- `du`、`df`、APFS 容器可用/可清除空间及快照可能重叠，不能相加为可释放容量；移动到同卷隔离目录/废纸篓也不能宣称已释放空间。
- 清理后用 `cleanup.py record` 保存操作者声明与文件系统实测，追加到任务 JSONL 日志。`observed_absent` 仅证明目标此时不在原处；同卷移动单独记录，无法归因的实际释放量保留 `null`。再补充应用/服务和保护对象验证，不把日志写入成功当成维护完成。
- 汇报已更新/已清理、已核实无需处理、未验证/受阻项目，附关键版本、空间证据和准确原因。认证、重启、项目迁移等剩余事项单独列出，不把“待处理”写成“已完成”。

简短交付示例：`浏览器 A 已由 3.8.0 更新为 3.8.1，重新启动后运行版本已核实；清理目标减少约 800MB，数据卷可用空间由 25.0GiB 变为 25.7GiB。系统当前通道无匹配更新。剪辑软件等待项目备份可访问；SSD 固件尚未取得型号匹配证据。`

调用示例：`使用 $mac-maintenance 只读检查这台 Mac。` 或 `按上次报告，用 $mac-maintenance 完成已授权的更新、整理和清理，并核实结果。`
